version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      -checkout
      - run: npm install
      - run: CI=true npm run build
  test:
    docker:
      - image: alpine:3.7
    steps:
      -checkout
      - run: npm install
      - run: npm test
workflows:
  version: 2.1
  build_test_and_lint:
  jobs:
    - build
    - test
